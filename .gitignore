# Pool de origem (PPT + extração) — não versionar
_source/
_extracao/

# Snapshots HTML do /stamp (pesados, git já é histórico)
_log/*.html

# Build legado (substituído por GitHub Action)
dist/

# PDFs gerados
*.pdf

# macOS
.DS_Store
**/.DS_Store

# Temporários
*.tmp
*.bak
